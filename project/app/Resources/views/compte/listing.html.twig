{% set _menu_active = 'compte' %}
{% extends 'base.html.twig' %}

{% block body %}
<table class="table table-striped table-condensed">
	<thead>
		<tr>
			<th></th>
			<th>Prénom</th>
			<th>Nom</th>
			<th>Tags</th>
			<th>Actif</th>
		</tr>
	</thead>
	<tbody>
		{% for compte in comptes %}
		<tr>
			<td><span class="glyphicon glyphicon-user" style="color: {{ compte.couleur }};"></span></td>
			<td>{{ compte.prenom }}</td>
			<td>{{ compte.nom }}</td>
			<td>{% for tag in compte.tags %}{{ tag.nom }}, {% endfor %}</td>
			<td><input class="switcher" type="checkbox" data-size="mini" data-url="{{ path('compte_update_etat', {id: compte.id }) }}" /></td>
		</tr>
		{% endfor %}
	</tbody>
</table>
{% endblock %}
