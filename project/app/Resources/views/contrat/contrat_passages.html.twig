<div class="panel panel-default">
    <div class="panel-heading">
        <strong>Interventions prévues</strong> (Acceptation du contrat le {{ contrat.dateAcceptation | date("d/m/y")}})  <span class='pull-right'>Date de début : <strong>{{ contrat.dateDebut | date("d/m/Y")}}</strong></span></div>
    <div class="panel-body">
        <ul class="list-inline list-group">
            {% if(contrat.etablissements | length) %}
                {% set firstEtbPassages = contrat.passagesEtablissementNode(contrat.etablissements | first) %}
                {% for passage in firstEtbPassages.passages %}
                    {% set counter = loop.index %}
                    <li class="col-xs-4">
                        <div class="panel panel-default">
                            <div class="panel-heading"><span class="badge pull-left">{{ counter }}</span>&nbsp;<span class="pull-right">prévu le <strong>{{ passage.datePrevision|date("d/m/Y") }}</strong></span></div>
                            <div class="panel-body">
                                <div class="row">
                                    <div class="col-xs-8">
                                        {% for prestation in passage.prestations %}
                                            {% if(prestation.wordToPicto) %}
                                                <span class="step size-24" title="{{  prestation.nomToString }}" data-toggle="tooltip" >
                                                    <i id="icon-{{prestation.wordToPicto}}" class="icon-{{prestation.wordToPicto}}">&nbsp;</i></span>                                                          
                                                {%else%}
                                                <span class="label label-xs label-primary">{{  prestation.nomToString }}</span>
                                            {%endif%}
                                        {% endfor %}
                                    </div>
                                    <div class="col-xs-4 text-right">
                                        {% if(passage.mouvementDeclenchable) %}
                                            <span class="label label-xs label-default">facture</span>
                                        {% endif %}
                                    </div>
                                </div>
                            </div>
                        </div>
                    </li>
                {% endfor %}
            {% endif %}
        </ul>
    </div>
</div>