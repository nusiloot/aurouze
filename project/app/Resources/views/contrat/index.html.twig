{% set _menu_active = 'contrat' %}
{% extends 'base.html.twig' %}
{% block title %}Contrats{% endblock title %}
{% block body %}
    <ol class="breadcrumb">
        <li><a href="{{ path('contrat') }}">Contrat</a></li>
    </ol>

    {{ include('societe/choixForm.html.twig', {'urlTarget': path('contrats_societe', {'id': '_id_'}), 'autofocus': true })}}

    <div class="row">
      <div class="col-xs-12 text-right">
        <a href="{{ path('contrats_reconduction_massive') }}" class="btn btn-default" ><span class="glyphicon glyphicon-refresh"></span>&nbsp;Reconduction massive</a>
      </div>
    </div>

    <h3>Derniers contrats créés</h3>

	{{ include('contrat/contratTableau.html.twig', {'contrats': contrats }) }}

{% endblock %}
